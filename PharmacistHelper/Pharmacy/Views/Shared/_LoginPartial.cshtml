@using Microsoft.AspNet.Identity
@if (Request.IsAuthenticated)
{
    using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new {id = "logoutForm", @class = "navbar-right"}))
    {
        @Html.AntiForgeryToken()

        <ul class="nav navbar-nav navbar-right">
            <li>
                @Html.ActionLink("Salve " + User.Identity.GetUserName() + "!", "Manage", "Account", null, new {title = "Gestione"})
            </li>
            <li>
                <a href="javascript:document.getElementById('logoutForm').submit()">Disconnetti</a>
            </li>
        </ul>
    }
}
else
{
    <ul class="nav navbar-nav navbar-right">
        <li>@Html.ActionLink("Registrazione", "Register", "Account", null, new {id = "registerLink"})</li>
        <li>@Html.ActionLink("Accedi", "Login", "Account", null, new {id = "loginLink"})</li>
    </ul>
}